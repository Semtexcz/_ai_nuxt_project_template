<script setup lang="ts">
const { count, doubleCount, increment } = useExample()
</script>

<template>
  <section class="home-page">
    <p class="eyebrow">{{ project_name }}</p>
    <h1>{{ project_description }}</h1>
    <p class="summary">
      Start from product intent, define the screen state, then implement one small vertical slice.
    </p>

    <div class="example-panel" aria-label="Example interaction">
      <p>Count: {% raw %}{{ count }}{% endraw %}</p>
      <p>Double count: {% raw %}{{ doubleCount }}{% endraw %}</p>
      <BaseButton @click="increment">
        Increment
      </BaseButton>
    </div>
  </section>
</template>

<style scoped>
.home-page {
  display: grid;
  gap: var(--spacing-lg);
}

.eyebrow {
  margin: var(--spacing-none);
  color: var(--color-primary);
  font-size: var(--font-size-small);
  font-weight: var(--font-weight-bold);
  text-transform: uppercase;
}

h1 {
  max-width: var(--layout-measure);
  margin: var(--spacing-none);
  font-size: var(--font-size-heading);
  line-height: var(--line-height-tight);
}

.summary {
  max-width: var(--layout-copy);
  margin: var(--spacing-none);
  color: var(--color-gray-600);
  font-size: var(--font-size-lead);
}

.example-panel {
  display: grid;
  max-width: var(--layout-panel);
  gap: var(--spacing-md);
  border: var(--border-width-sm) solid var(--color-gray-300);
  border-radius: var(--radius-lg);
  padding: var(--spacing-lg);
  background: var(--color-white);
  box-shadow: var(--shadow-subtle);
}

.example-panel p {
  margin: var(--spacing-none);
}
</style>
